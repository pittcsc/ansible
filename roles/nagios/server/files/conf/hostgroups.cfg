define hostgroup {
  hostgroup_name  all
	alias           All Servers
	members         *
}

define service {
  use                   generic-service
  hostgroup_name        all
  service_description   SSH
  check_command         check_ssh
}
